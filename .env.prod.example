# Infrastructure Data Mounts (Production)
# Change this to the absolute path of the external volume on your production host
FOREST_DATA_ROOT=/mnt/volume_ams3_01/forest-res

# Security (Production MUST change this)
JWT_SECRET=super_secure_random_string_here

# Mapbox (Required for production build if building inside docker, or passed at runtime)
# NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_mapbox_token_here
